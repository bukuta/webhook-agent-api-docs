Action:
  type: object
  description: 动作
  properties:
    id:
      type: integer
      x-mock: '@integer'
    type:
      $ref: '#/components/schemas/ActionType'
    project:
      type: integer
      x-mock: '@integer'
      x-$ref: '#/components/schemas/Project'
    hook:
      type: integer
      x-mock: '@integer'
    creator:
      type: integer
      description: 创建人
      x-$ref: '#/components/schemas/Admin'
      x-mock: '@integer'
    createAt:
      type: string
      format: date-time
      description: 创建时间
      x-mock: '@datetime'
    updateAt:
      type: string
      format: date-time
      description: 更新时间
      x-mock: '@datetime'

