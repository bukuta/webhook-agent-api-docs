Hook:
  type: object
  properties:
    id:
      $ref: '#/components/schemas/ID'
    name:
      type: string
      x-mock: '@string'
    type:
      type: string
      x-mock: '@string'

    creator:
      x-$ref: '#/components/schemas/DeviceType'
      $ref: '#/components/schemas/ID'
    createAt:
      type: string
      format: date-time
      x-mock: '@datetime'
    updateAt:
      type: string
      format: date-time
      x-mock: '@datetime'

