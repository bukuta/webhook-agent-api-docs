Log:
  type: object
  description: 日志
  properties:
    id:
      $ref: '#/components/schemas/ID'
    content:
      type: string
      x-mock: '@cparagraph'
    task:
      $ref: '#/components/schemas/ID'
      x-$ref: '#/components/schemas/Task'

    createAt:
      type: string
      format: date-time
      x-mock: '@datetime'
    updateAt:
      type: string
      format: date-time
      x-mock: '@datetime'
